// Measuring up - Measure Formulas


// Measure Formulas


// You can multiply measures by separating two measures with a space or 
// an asterisk (*) to create a product measur

[<Measure>] type lb
[<Measure>] type ft
[<Measure>] type lbft = lb ft

// You can divide measures by separating two measures with a forward 
// slash (/) to create a quotient measure. F


[<Measure>] type m
[<Measure>] type h
[<Measure>] type mph = m / h

// Positive and negative integral values can be used to express an
// exponential relationship between two measures.

[<Measure>] type sqft = ft ^ 2
